'use strict';

angular.module('cameraboxApp')
  .controller('MainCtrl', function ($scope) {
    $scope.awesomeThings = [
      'RESTX',
      'Twitter Bootstrap',
      'AngularJS',
      'Karma'
    ];
  });
